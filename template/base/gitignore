# deps / builds
node_modules/
dist/
dist-types/
# Rust build output (src-tauri/.gitignore is folded in here by convention)
target/
# tauri-build regenerates these ACL/capability schemas on every cargo build
apps/desktop/src-tauri/gen/
*.tsbuildinfo
coverage/

# env & local secrets — .env.example is the committed contract
.env
.env.*
!.env.example
# dev-only JWKS + minted tokens (apps/server/scripts/mint-dev-token.mjs)
.dev-auth/

# harness runtime state (gate stamps, caches) — the install manifest stays
# committed so `doctor` and CI can hash-verify harness-owned files
.harness/*
!.harness/manifest.json

# editor/os noise
.DS_Store
*.log
.eslintcache
