{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main window: core defaults plus logging. Least privilege — no fs, no shell, no http, no remote URLs. Add narrower allow-* permissions here as commands grow; check-tauri-policy and the security-reviewer audit every diff.",
  "windows": ["main"],
  "permissions": ["core:default", "log:default"]
}
