{
  "comment": "Falsifiability registry: every step in VALIDATE_STEPS ∪ STOP_HOOK_STEPS must carry at least one mechanical proof that it CAN go red. Proof kinds: 'fixture' (a repo test file that spawns the real gate and asserts exit 1 — ref must exist), 'selftest' (a literal step-name string that must appear in .github/workflows/selftest.yml), 'runner' (the step wraps a vendor tool whose non-zero exit is the red; OUR wiring's failure propagation is proven by the validate-runner tests — ref must exist). scripts/check-canary-coverage.mjs enforces bidirectional closure: an unregistered step OR a stale registry entry reds the PR that caused it. Guard-rule falsifiability is now a PER-RULE closure, not a count: check-canary-coverage.mjs imports the pure-data rule tables from .claude/hooks/lib/guard-rules.mjs (BASH_RULES, WRITE_PROTECTED, WRITE_GLOBAL_CHECKS) and asserts every rule id has a behavioral deny/allow case (a RULE_CANARIES entry) in tests/hooks/hook-contract.test.mjs — a rule added without a canary reds the PR. hookRules below keeps only a human-readable grep-able deny example per surface.",
  "steps": {
    "format": [{ "kind": "runner", "ref": "tests/gates/validate-runner.test.mjs", "note": "biome ci exits non-zero on any diff" }],
    "gate-integrity": [{ "kind": "fixture", "ref": "tests/gates/check-gate-integrity.test.mjs" }],
    "rust-fmt": [{ "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" }],
    "types": [{ "kind": "runner", "ref": "tests/gates/validate-runner.test.mjs", "note": "tsc -b exits non-zero on type errors" }],
    "lint": [{ "kind": "runner", "ref": "tests/gates/validate-runner.test.mjs", "note": "eslint --max-warnings 0 exits non-zero on any finding" }],
    "provenance": [
      { "kind": "selftest", "ref": "Canary 3: strip SOURCE citations from decision sites → provenance must FAIL" },
      { "kind": "fixture", "ref": "tests/gates/check-sources.test.mjs" },
      { "kind": "fixture", "ref": "tests/gates/check-sources.test.mjs", "note": "wrong-group corpus cite on a decision site → provenance red naming site, group, and cited groups; tools/provenance-overrides.json is the reviewed escape; pre-0.1.5 baseVersion manifests downgrade to a ramp NOTE" },
      { "kind": "fixture", "ref": "tests/gates/check-sources.test.mjs", "note": "bare-URL SOURCE on a host outside tools/lib/citation-domains.mjs → provenance red naming the host; same pre-0.1.5 ramp NOTE" }
    ],
    "tauri-policy": [{ "kind": "selftest", "ref": "Canary 1: weaken tauri.conf CSP to null → tauri-policy must FAIL" }],
    "version-sync": [{ "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" }],
    "prompts": [
      { "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" },
      { "kind": "selftest", "ref": "Canary 7: tamper a hash-locked prompt → prompts must FAIL" }
    ],
    "licenses": [{ "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" }],
    "schema-rls": [
      { "kind": "selftest", "ref": "Canary 4: remove FORCE RLS from the migration → schema-rls must FAIL" },
      { "kind": "fixture", "ref": "tests/gates/check-rls-manifest.test.mjs" }
    ],
    "migrations": [{ "kind": "selftest", "ref": "Canary 2: edit an existing migration → migrations must FAIL" }],
    "contracts": [{ "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" }],
    "dead-code": [{ "kind": "runner", "ref": "tests/gates/validate-runner.test.mjs", "note": "knip --strict exits non-zero on unused exports/files" }],
    "architecture": [{ "kind": "runner", "ref": "tests/gates/validate-runner.test.mjs", "note": "depcruise exits non-zero on rule violations; the DAL-boundary rules were proven firing when added" }],
    "build": [
      { "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" },
      { "kind": "fixture", "ref": "tests/gates/build-check.test.mjs", "note": "gzip ratchet: a committed tools/perf-baseline.json with a tiny total → build red naming measured vs baseline × ratioCap and the `pnpm perf:baseline` re-baseline ceremony; exactly AT the cap stays green" },
      { "kind": "fixture", "ref": "tests/gates/build-check.test.mjs", "note": "self-disable + fail-closed: absent baseline → NOTE naming the file and `pnpm perf:baseline` while the absolute byte budgets keep their 0.1.4 behavior; malformed baseline JSON/shape → red, never fail-open" }
    ],
    "rust-check": [{ "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" }],
    "styleguide": [
      { "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" },
      { "kind": "fixture", "ref": "tests/gates/check-styleguide-manifest.test.mjs" },
      { "kind": "fixture", "ref": "tests/gates/check-styleguide-manifest.test.mjs", "note": "primitive boundary: a hand-rolled <button className=…> outside the controlPrimitives home → red with the FIX line naming the Button/Input primitive; controlAllow (file + reason) is the reviewed escape and malformed/stale entries fail closed; a manifest without controlPrimitives self-disables with the update --refresh-seeded adoption NOTE" },
      { "kind": "fixture", "ref": "tests/gates/check-styleguide-manifest.test.mjs", "note": "AAA tier: an ink between the AA and AAA boundaries (~6.2:1 on the light canvas) → red printing '(min 7:1)' — the body-text pairs (ink/canvas, ink/surface) enforce 7:1 per-pair while ink-muted/accent stay at the honest 4.5 floor, so the red can only come from the raised min" },
      { "kind": "selftest", "ref": "Canary 5: raw hex + erased palette utility in a TSX → styleguide must FAIL" }
    ],
    "perf-budget": [
      { "kind": "fixture", "ref": "tests/gates/gate-canary-misc.test.mjs" },
      { "kind": "fixture", "ref": "tests/gates/check-perf-budget.test.mjs" },
      { "kind": "fixture", "ref": "tests/gates/check-perf-budget.test.mjs", "note": "dense-feature closure: a features/* dir importing useVirtualWindow/useRovingGrid with no perfSubject.ts → red with the create-FIX line naming the worked matrix pattern; exempt[] is the reviewed escape and malformed/stale exempt entries fail closed" },
      { "kind": "fixture", "ref": "tests/gates/check-perf-budget.test.mjs", "note": "subjects[] closure inverse: an entry pointing at a missing file and an undeclared features/*/perfSubject.ts both red before any measurement; declaring subject AND subjects together is an ambiguity red; legacy single-subject budgets keep 0.1.4 behavior plus a NOTE naming the refresh command" }
    ],
    "route-manifest": [
      { "kind": "fixture", "ref": "tests/gates/check-route-manifest.test.mjs" },
      { "kind": "selftest", "ref": "Canary 6: unregistered features directory → route-manifest must FAIL" }
    ],
    "e2e": [{ "kind": "fixture", "ref": "tests/gates/check-e2e.test.mjs" }],
    "docs-sync": [
      { "kind": "fixture", "ref": "tests/gates/check-docs-sync.test.mjs" },
      { "kind": "fixture", "ref": "tests/gates/check-docs-sync.test.mjs", "note": "renaming/removing a numbered gates-catalog section reds the catalog-lockstep sub-check; a pre-0.1.5 baseVersion manifest downgrades it to a ramp NOTE" },
      { "kind": "fixture", "ref": "tests/gates/check-docs-sync.test.mjs", "note": "agent roster: granting a reviewer Write or Bash, dropping its disallowedTools, omitting its tools list, or unparseable frontmatter each red the roster sub-check — reviewers are read-only by construction; deliberately NOT ramped (the roster is harness-owned and refreshes with the gate in the same update)" }
    ],
    "validate": [{ "kind": "fixture", "ref": "tests/gates/validate-runner.test.mjs" }],
    "rls-isolation": [{ "kind": "selftest", "ref": "RLS runner FAILS CLOSED without a database (CI=true)" }],
    "unit": [{ "kind": "runner", "ref": "tests/gates/validate-runner.test.mjs", "note": "vitest exits non-zero on failing tests or coverage-threshold misses" }],
    "diff-coverage": [{ "kind": "fixture", "ref": "tests/gates/check-diff-coverage.test.mjs", "note": "an uncovered new src file (absent from coverage-final.json) and a below-floor changed file both red the Stop chain naming file + metric; missing coverage-final.json fails closed" }]
  },
  "hookRules": {
    "pretool-bash-guard.mjs": {
      "denyToolCallSites": 1,
      "denyExamples": ["rm -rf node_modules", "git push --force origin main", "psql \"$MIGRATOR_DATABASE_URL\" -c \"select 1\""]
    },
    "pretool-write-guard.mjs": {
      "denyToolCallSites": 10,
      "denyExamples": ["tools/harness.config.mjs", "dangerouslySetInnerHTML", "tools/route-allowlist.json"]
    }
  }
}
